# Continuously replicates the SQLite history to Cloud Storage (Cloud Run disks are ephemeral).
dbs:
  - path: ${BEST_DEAL_DB}
    replicas:
      - url: ${LITESTREAM_REPLICA_URL}
        sync-interval: 1s
